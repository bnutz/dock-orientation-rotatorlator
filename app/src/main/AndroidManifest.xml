<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <uses-permission
        android:name="android.permission.WRITE_SETTINGS"
        tools:ignore="ProtectedPermissions"
        />

    <!-- Required since targetSdk 28 for startForeground(); SPECIAL_USE variant since API 34 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- API 33+: the service's status notification is invisible without this runtime grant -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rotatorlator"
        >
        <!-- Single Compose Activity: system splash (androidx.core.splashscreen) + the
             WRITE_SETTINGS gate + the config screen. singleTask so the notification's
             content Intent brings the existing instance forward. -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Rotatorlator.Splash"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Receiver to restart ServicePortStatusHandler after app is updated
             (power/dock broadcasts themselves are received via a dynamically registered
             receiver inside PowerStateRepository's Flow, not a manifest entry) -->
        <receiver
            android:name=".service.ReceiverRestartServiceAfterUpdate"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Services. specialUse: no standard FGS type covers "watch charger/dock state";
             the subtype property below is what a Play policy review reads. -->
        <service
            android:name=".service.ServicePortStatusHandler"
            android:foregroundServiceType="specialUse"
            >
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Monitors charger/dock connect and disconnect events so the app can apply the user's chosen screen-rotation lock setting"
                />
        </service>

    </application>

</manifest>
